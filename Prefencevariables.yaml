- name: arngvariable
  hosts: local
  connection: local
  vars_prompt:
    - name: username
      prompt: what is your name?
      private: false
  vars_files:
   - vars.yaml
  tasks:
   - name: arngvariables
     vars:
       username : Playvariables
     ansible.builtin.debug:
       msg: "print : {{username}}"