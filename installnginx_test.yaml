- name: install nginx
  hosts: test
  become: yes
  tasks:
     - name: install nginx in radhat
       ansible.builtin.dnf:
         name: nginx
         state: present
       when: ansible_os_family == "RedHat"
       


